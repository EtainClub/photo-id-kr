{
  "name": "passport-ready-kr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "build": "node build.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check analyzer.js && node --test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
